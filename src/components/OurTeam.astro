---
import PlaceholderProfile from "../assets/svgs/placeholder-profile.svg";
const { t } = Astro.locals;
---

<div class="py-30 md:py-38 max-md:px-4">
  <div class="flex flex-col items-center mx-auto max-w-7xl gap-y-30">
    <div class="flex flex-col gap-12 items-center mx-auto max-w-145">
      <h2 class="text-[38px] md:text-5xl font-bold text-center leading-[normal] max-md:max-w-100">
        <span class="inline px-4 text-white bg-dark line-box">
          {t("team.heading")}
        </span>
      </h2>

      <p class="text-2xl font-bold text-center">
        {t("team.subtitle")}
      </p>
    </div>

    <div class="flex flex-wrap max-md:justify-center max-md:items-center gap-x-20 gap-y-15 md:gap-25">
      <div class="flex flex-col gap-6 md:gap-8 items-center flex-1 md:w-50">
        <PlaceholderProfile class="size-30 md:size-50 aspect-square" />

        <div class="flex flex-col gap-y-2 md:gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>

      <div class="flex flex-col gap-6 md:gap-8 items-center flex-1 md:w-50">
        <PlaceholderProfile class="size-30 md:size-50 aspect-square" />

        <div class="flex flex-col gap-y-2 md:gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>

      <div class="flex flex-col gap-6 md:gap-8 items-center flex-1 md:w-50">
        <PlaceholderProfile class="size-30 md:size-50 aspect-square" />

        <div class="flex flex-col gap-y-2 md:gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>

      <div class="flex flex-col gap-6 md:gap-8 items-center flex-1 md:w-50">
        <PlaceholderProfile class="size-30 md:size-50 aspect-square" />

        <div class="flex flex-col gap-y-2 md:gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>
    </div>
  </div>
</div>
