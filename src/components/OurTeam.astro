---
import PlaceholderProfile from '../assets/svgs/placeholder-profile.svg';
const { t } = Astro.locals;
---

<div class="py-38">
  <div class="flex flex-col items-center mx-auto max-w-7xl gap-y-30">
    <div class="flex flex-col gap-12 items-center mx-auto max-w-145">
      <h2 class="text-5xl font-bold text-center">
        <span class="inline px-4 leading-[normal] text-white bg-dark line-box">
          {t("team.heading")}
        </span>
      </h2>

      <p class="text-2xl font-bold text-center">
        {t("team.subtitle")}
      </p>
    </div>

    <div class="flex gap-25">
      <div class="flex flex-col gap-8 items-center w-50">
        <PlaceholderProfile class="size-50 aspect-square" />

        <div class="flex flex-col gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>
      
      <div class="flex flex-col gap-8 items-center w-50">
        <PlaceholderProfile class="size-50 aspect-square" />

        <div class="flex flex-col gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>
      
      <div class="flex flex-col gap-8 items-center w-50">
        <PlaceholderProfile class="size-50 aspect-square" />

        <div class="flex flex-col gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>
      
      <div class="flex flex-col gap-8 items-center w-50">
        <PlaceholderProfile class="size-50 aspect-square" />

        <div class="flex flex-col gap-y-3 items-center">
          <p class="font-serif text-lg font-bold">John Doe</p>
          <p class="font-serif">{t("team.roleCEO")}</p>
        </div>
      </div>
    </div>
  </div>
</div>
